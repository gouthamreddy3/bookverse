Feature modules live here (e.g. `books/`, `reviews/`, `profiles/`).

Each feature module owns its own components, server actions, queries, and types.
Only cross-feature, reusable code belongs in `src/components` and `src/lib`.
